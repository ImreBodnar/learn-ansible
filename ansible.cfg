[defaults]
# inventory = hosts
inventory = inventory_aws_ec2.yaml
host_key_checking = False

# A dynamic inventory példához kellenek ezek
enable_plugins = aws_ec2
remote_user = ec2-user
private_key_file = ~/.ssh/aws-ec2-tf