---
- name: Configure nginx web server
  hosts: ec2
  become: true

  tasks: 
  - name: install nginx server
    dnf:
      name: nginx
      state: latest
  - name: start nginx server
    service:
      name: nginx
      state: started